# Local Git-backed Change Implement Prompt

Legacy stable path. The active worker prompt template is owned by the repo skill at `.agents/skills/formless-git-change-apply/templates/local-implement.md`.

Do not maintain workflow rules here.
