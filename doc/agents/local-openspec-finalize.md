# Local Git-backed Finalize Prompt

Legacy stable path. The active finalization prompt template is owned by the repo skill at `.agents/skills/formless-git-change-finalize/templates/local-finalize.md`.

Do not maintain workflow rules here.
